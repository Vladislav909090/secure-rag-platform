run:
  timeout: 3m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - ineffassign
    - nlreturn
    - revive

linters-settings:
  nlreturn:
    block-size: 2
  errcheck:
    check-type-assertions: true
  govet:
    enable-all: true
    disable:
      - fieldalignment
  revive:
    rules:
      - name: exported
        severity: warning
      - name: var-naming
        severity: warning
      - name: indent-error-flow
        severity: warning
      - name: cognitive-complexity
        arguments: [40]
      - name: cyclomatic
        arguments: [25]

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
